class ApiConfig {
  ApiConfig._();

  static const baseUrl = 'https://api.goatech.ma';
  static const tenantSchema = 'mocca';
  static const connectTimeout = Duration(seconds: 30);
  static const receiveTimeout = Duration(seconds: 30);
}
