import 'app_bootstrap.dart';
import 'core/app_flavor.dart';

/// Manager Rapport (JTR Mobile) entry (`--flavor rapport -t lib/main_rapport.dart`).
Future<void> main() => bootstrapApp(AppFlavor.rapport);
